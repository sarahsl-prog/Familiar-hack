# Handoff

## State
Running `/prd` for NanoClaw × Grimoire Discord bot. `docs/scope.md` and `docs/learner-profile.md` are complete. `process-notes.md` has a `## /prd` section started (in progress). No `docs/prd.md` yet — mid-interview.

## Next
1. Resume the mandatory questions interview for `/prd`. We covered: (1) `!ask` response = markdown prose, 1-2 paragraphs, source docs cited at end (Grimoire returns this natively). (2) `!search` response = list of 5 relevant documents. **Stopped here** — next question was what each list item contains (title only? description? relevance score? file path?).
2. Continue through remaining mandatory questions, then offer deepening rounds.
3. Generate `docs/prd.md` when ready.

## Context
Sarah is intermediate level. She values explicit over clever (drove the `!ask`/`!search` prefix decision). Grimoire's API already returns formatted output — the tool is a thin connector, not a new pipeline. Keep redirecting any tech questions to `/spec`.
